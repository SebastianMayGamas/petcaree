// sendNotification.js
/*const admin = require('firebase-admin');

// Carga el archivo de credenciales JSON
const serviceAccount = require('ruta/a/tu/archivo-de-credenciales.json');

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount),
});

// Define el mensaje
const message = {
  notification: {
    title: 'Nueva publicación en Petcare',
    body: 'Haz clic para ver la nueva publicación.',
  },
  topic: 'allUsers', // O usa el token del dispositivo del usuario
};

// Envía el mensaje
admin.messaging().send(message)
  .then((response) => {
    console.log('Mensaje enviado con éxito:', response);
  })
  .catch((error) => {
    console.error('Error al enviar el mensaje:', error);
  });
*/